import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  errorText: {
    color: 'red',
  },
  noDataText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'red',
  },
});

export default styles;
