import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  scrollContainer: {
   
  },

  errorText: {

    color: 'red',
    
  },
  noDataText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'red',
  },
});

export default styles;
